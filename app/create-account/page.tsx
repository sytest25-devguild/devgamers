import CreateAccountPhaser from "@/components/CreateAccountPhaser";

export default function CreateAccountPage() {
  return (
    <main className="landing-page">
      <CreateAccountPhaser />
    </main>
  );
}